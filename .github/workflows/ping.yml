name: Keep Render Site Awake

on:
  schedule:
    - cron: '*/14 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Sites
        run: |
          echo "Pinging main website..."
          curl -I https://jabir2233.onrender.com

          echo "Pinging number guessing game..."
          curl -I https://jabir2233-number-guessing-game.onrender.com
